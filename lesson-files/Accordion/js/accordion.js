$(document).ready(function(){
  // hide all ULs inside LI.drawer except the first one
    
    // Add a click handler to the H2 elements

        // hide all the drawer contents and then
	   // remove the class .open from all H2 elements (previous DOM node to the UL)

        // Add the class 'open' to the clicked H2 (this), then
        // show the associated drawer content (next) with the slideDown() function

    // end the click handler
});
